---
description: Show TokenTrader earnings, impressions, and gift card progress
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/token-trader status` using the Bash tool and display the output verbatim in your response. Do not summarize or reformat — the CLI output is already formatted for display.
